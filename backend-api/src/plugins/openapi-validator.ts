/**
 * T049: Optional strict OpenAPI response validation (not wired). Enable when responses
 * must be checked against `specs/001-tharwa-platform-mvp/contracts/openapi.yaml` at runtime.
 */
export const OPENAPI_VALIDATOR_PLACEHOLDER = true;
